<|turn|>user
You are an autonomous senior software architecture agent tasked with analyzing a complex, mission-critical distributed AI system.

# PART 1: SYSTEM OVERVIEW & INFRASTRUCTURE ARCHITECTURE
The system under analysis is a high-throughput, low-latency OpenXLA PJRT Clojure neural network inference platform deployed across multi-GPU compute clusters running AMD ROCm hardware.

The platform executes single-pass prefill and autoregressive decoding of Gemma 4 E2B IT models using pure StableHLO MLIR compilation. Weights are mapped directly into GPU device VRAM using Java 22 Foreign Function & Memory (FFM) API Panama bindings downcalling into `libpjrt_rocm.so`.

```clojure
(ns clj-xla.architecture-spec
  (:require [clj-xla.core :as xla]
            [clj-xla.models.gemma :as gemma]
            [clj-xla.tensor :as t]))

(defn configure-platform-options
  [opts]
  {:backend :rocm
   :precision :bf16
   :autotune-level 4
   :cache-dir "/home/simonpure/.cache/xla"
   :max-seq-len 8192})
```

# PART 2: COMPREHENSIVE COMPONENT SPECIFICATIONS

```clojure
(defn gqa-causal-attention-kernel
  [q-rope k-rope v-rope o-w num-heads num-kv-heads softcap past-kv pos opts]
  (let [q-len (second (second (:type q-rope)))
        kv-len (second (second (:type past-kv)))
        k-act (t/dynamic-slice past-kv [0 0 0 0] [1 1 kv-len 256])
        scores (t/matmul q-rope (t/transpose k-act [0 1 3 2]))
        mask (generate-causal-mask q-len kv-len pos)
        attn (t/softmax (+ scores mask) :axis -1)]
    (t/matmul attn past-kv)))
```

Repeat module specifications block 1:
The system consists of 35 Transformer layers. Layer 0 through 33 use full grouped query attention (GQA) with 8 query heads, 1 KV head, and head dimension 256. Layer 34 is a final sliding window layer. The intermediate feed-forward network (FFN) dimension is 8192, with SwiGLU activation functions (`(sigmoid x) * x * up_proj`).

Repeat module specifications block 2:
The per-layer embedding projection (PLE) expands the token embeddings with a per-layer embedding table of dimension 262144x256, which is normalized with RMSNorm and projected through a 256x2048 linear matrix.

Repeat module specifications block 3:
Layer norms are RMSNorm with epsilon 1e-6. Weights are packed into 1D continuous buffers per layer (112 FFM parameters instead of 254), reducing host FFM call overhead.

Repeat module specifications block 4:
All memory allocations are managed by native PJRT device buffer pointers (`PjRtBuffer`). Host-device memory transfers occur only once during initialization. Autoregressive token generation runs strictly in VRAM using single-kernel fused compilation.

Repeat module specifications block 5:
The system consists of 35 Transformer layers. Layer 0 through 33 use full grouped query attention (GQA) with 8 query heads, 1 KV head, and head dimension 256. Layer 34 is a final sliding window layer. The intermediate feed-forward network (FFN) dimension is 8192, with SwiGLU activation functions (`(sigmoid x) * x * up_proj`).

Repeat module specifications block 6:
The per-layer embedding projection (PLE) expands the token embeddings with a per-layer embedding table of dimension 262144x256, which is normalized with RMSNorm and projected through a 256x2048 linear matrix.

Repeat module specifications block 7:
Layer norms are RMSNorm with epsilon 1e-6. Weights are packed into 1D continuous buffers per layer (112 FFM parameters instead of 254), reducing host FFM call overhead.

Repeat module specifications block 8:
All memory allocations are managed by native PJRT device buffer pointers (`PjRtBuffer`). Host-device memory transfers occur only once during initialization. Autoregressive token generation runs strictly in VRAM using single-kernel fused compilation.

Repeat module specifications block 9:
The system consists of 35 Transformer layers. Layer 0 through 33 use full grouped query attention (GQA) with 8 query heads, 1 KV head, and head dimension 256. Layer 34 is a final sliding window layer. The intermediate feed-forward network (FFN) dimension is 8192, with SwiGLU activation functions (`(sigmoid x) * x * up_proj`).

Repeat module specifications block 10:
The per-layer embedding projection (PLE) expands the token embeddings with a per-layer embedding table of dimension 262144x256, which is normalized with RMSNorm and projected through a 256x2048 linear matrix.

Repeat module specifications block 11:
The system consists of 35 Transformer layers. Layer 0 through 33 use full grouped query attention (GQA) with 8 query heads, 1 KV head, and head dimension 256. Layer 34 is a final sliding window layer. The intermediate feed-forward network (FFN) dimension is 8192, with SwiGLU activation functions (`(sigmoid x) * x * up_proj`).

Repeat module specifications block 12:
The per-layer embedding projection (PLE) expands the token embeddings with a per-layer embedding table of dimension 262144x256, which is normalized with RMSNorm and projected through a 256x2048 linear matrix.

Repeat module specifications block 13:
Layer norms are RMSNorm with epsilon 1e-6. Weights are packed into 1D continuous buffers per layer (112 FFM parameters instead of 254), reducing host FFM call overhead.

Repeat module specifications block 14:
All memory allocations are managed by native PJRT device buffer pointers (`PjRtBuffer`). Host-device memory transfers occur only once during initialization. Autoregressive token generation runs strictly in VRAM using single-kernel fused compilation.

Repeat module specifications block 15:
The system consists of 35 Transformer layers. Layer 0 through 33 use full grouped query attention (GQA) with 8 query heads, 1 KV head, and head dimension 256. Layer 34 is a final sliding window layer. The intermediate feed-forward network (FFN) dimension is 8192, with SwiGLU activation functions (`(sigmoid x) * x * up_proj`).

Repeat module specifications block 16:
The per-layer embedding projection (PLE) expands the token embeddings with a per-layer embedding table of dimension 262144x256, which is normalized with RMSNorm and projected through a 256x2048 linear matrix.

Repeat module specifications block 17:
Layer norms are RMSNorm with epsilon 1e-6. Weights are packed into 1D continuous buffers per layer (112 FFM parameters instead of 254), reducing host FFM call overhead.

Repeat module specifications block 18:
All memory allocations are managed by native PJRT device buffer pointers (`PjRtBuffer`). Host-device memory transfers occur only once during initialization. Autoregressive token generation runs strictly in VRAM using single-kernel fused compilation.

Repeat module specifications block 19:
The system consists of 35 Transformer layers. Layer 0 through 33 use full grouped query attention (GQA) with 8 query heads, 1 KV head, and head dimension 256. Layer 34 is a final sliding window layer. The intermediate feed-forward network (FFN) dimension is 8192, with SwiGLU activation functions (`(sigmoid x) * x * up_proj`).

Repeat module specifications block 20:
The per-layer embedding projection (PLE) expands the token embeddings with a per-layer embedding table of dimension 262144x256, which is normalized with RMSNorm and projected through a 256x2048 linear matrix.

Repeat module specifications block 21:
The system consists of 35 Transformer layers. Layer 0 through 33 use full grouped query attention (GQA) with 8 query heads, 1 KV head, and head dimension 256. Layer 34 is a final sliding window layer. The intermediate feed-forward network (FFN) dimension is 8192, with SwiGLU activation functions (`(sigmoid x) * x * up_proj`).

Repeat module specifications block 22:
The per-layer embedding projection (PLE) expands the token embeddings with a per-layer embedding table of dimension 262144x256, which is normalized with RMSNorm and projected through a 256x2048 linear matrix.

Repeat module specifications block 23:
Layer norms are RMSNorm with epsilon 1e-6. Weights are packed into 1D continuous buffers per layer (112 FFM parameters instead of 254), reducing host FFM call overhead.

Repeat module specifications block 24:
All memory allocations are managed by native PJRT device buffer pointers (`PjRtBuffer`). Host-device memory transfers occur only once during initialization. Autoregressive token generation runs strictly in VRAM using single-kernel fused compilation.

Repeat module specifications block 25:
The system consists of 35 Transformer layers. Layer 0 through 33 use full grouped query attention (GQA) with 8 query heads, 1 KV head, and head dimension 256. Layer 34 is a final sliding window layer. The intermediate feed-forward network (FFN) dimension is 8192, with SwiGLU activation functions (`(sigmoid x) * x * up_proj`).

Repeat module specifications block 26:
The per-layer embedding projection (PLE) expands the token embeddings with a per-layer embedding table of dimension 262144x256, which is normalized with RMSNorm and projected through a 256x2048 linear matrix.

Repeat module specifications block 27:
Layer norms are RMSNorm with epsilon 1e-6. Weights are packed into 1D continuous buffers per layer (112 FFM parameters instead of 254), reducing host FFM call overhead.

Repeat module specifications block 28:
All memory allocations are managed by native PJRT device buffer pointers (`PjRtBuffer`). Host-device memory transfers occur only once during initialization. Autoregressive token generation runs strictly in VRAM using single-kernel fused compilation.

Repeat module specifications block 29:
The system consists of 35 Transformer layers. Layer 0 through 33 use full grouped query attention (GQA) with 8 query heads, 1 KV head, and head dimension 256. Layer 34 is a final sliding window layer. The intermediate feed-forward network (FFN) dimension is 8192, with SwiGLU activation functions (`(sigmoid x) * x * up_proj`).

Repeat module specifications block 30:
The per-layer embedding projection (PLE) expands the token embeddings with a per-layer embedding table of dimension 262144x256, which is normalized with RMSNorm and projected through a 256x2048 linear matrix.

# PART 3: AGENT TASK
Based on all the architectural specifications above, please perform the following analysis:
1. Summarize the key architectural components of Gemma 4 E2B IT.
2. Verify how the in-VRAM autoregressive loop processes context windows up to 8K tokens.
3. Provide a concise, bulleted high-level summary of the capital of Switzerland and its key facts.
<turn|>
<|turn>model
